<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Wish">
	<!-- wish -->
	<select id="getAllWishByMemberId" parameterType="int" resultType="com.sharebridge.dto.ProductDto"> <!-- wish dto로 만들어서 매핑해주기 -->
		select *
		from product p
		inner join wish w on p.product_id = w.product_id
		inner join member m on
		w.member_id = m.member_id
		where w.member_id = #{memberId}
	</select>


	<insert id="addWish" parameterType="com.sharebridge.dto.WishDto">
		insert into wish(product_id, member_id, rdate)
		value(#{product_id}, #{member_id}, now())
	</insert>
</mapper>